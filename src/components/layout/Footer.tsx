import React, { useState } from 'react';
import { MapPin, Mail, Phone } from 'lucide-react';
import { useStore } from '../../context/StoreContext';
import { BRAND, BrandMark } from '../../config/brand';

export const Footer: React.FC = () => {
  const { navigate, showToast } = useStore();
  const [email, setEmail] = useState('');
  const subscribe = (event: React.FormEvent) => { event.preventDefault(); showToast('Thanks for your interest', 'Newsletter delivery will be enabled after the email service is configured.', 'info'); setEmail(''); };
  const Link = ({ view, children }: { view: Parameters<typeof navigate>[0]; children: React.ReactNode }) => <button onClick={() => navigate(view)} className="text-left text-xs text-stone-300 transition hover:text-white">{children}</button>;
  return <footer className="bg-[#272421] text-[#fffdf9]"><div className="mx-auto grid max-w-[1440px] gap-10 px-4 py-14 sm:px-7 md:grid-cols-2 lg:grid-cols-5"><section className="lg:col-span-2"><BrandMark inverse className="text-xl" /><p className="mt-4 max-w-sm text-sm leading-6 text-stone-300">A boutique collection from Miyapur, Hyderabad. Brand story and product details will be published after client approval.</p><div className="mt-5 space-y-2 text-xs text-stone-300"><a className="flex items-start gap-2 hover:text-white" href={BRAND.mapsUrl} target="_blank" rel="noreferrer"><MapPin className="mt-0.5 h-4 w-4 shrink-0" />{BRAND.addressLines.slice(0, 4).join(', ')}</a><a className="flex items-center gap-2 hover:text-white" href={BRAND.whatsappUrl} target="_blank" rel="noreferrer"><Phone className="h-4 w-4" />{BRAND.phone}</a><a className="flex items-center gap-2 hover:text-white" href={`mailto:${BRAND.email}`}><Mail className="h-4 w-4" />{BRAND.email}</a></div></section><section className="space-y-3"><h2 className="text-[11px] font-semibold uppercase tracking-[.16em] text-stone-100">Shop</h2><div className="grid gap-2"><Link view="shop">Shop</Link><Link view="shop">Collections</Link><Link view="shop">New Arrivals</Link><Link view="tailoring-guide">Tailoring</Link></div></section><section className="space-y-3"><h2 className="text-[11px] font-semibold uppercase tracking-[.16em] text-stone-100">Customer Care</h2><div className="grid gap-2"><Link view="contact">Contact</Link><Link view="shipping-policy">Shipping</Link><Link view="returns-policy">Returns</Link><Link view="order-tracking">Order Tracking</Link><Link view="tailoring-guide">Size Guide</Link></div></section><section className="space-y-3"><h2 className="text-[11px] font-semibold uppercase tracking-[.16em] text-stone-100">Stay in touch</h2><p className="text-xs leading-5 text-stone-300">Newsletter delivery is not configured yet.</p><form onSubmit={subscribe} className="space-y-2"><input value={email} onChange={(event) => setEmail(event.target.value)} type="email" required placeholder="Email address" className="w-full border border-stone-600 bg-transparent px-3 py-2.5 text-xs outline-none focus:border-stone-200" /><button className="w-full border border-stone-100 px-3 py-2.5 text-[11px] font-semibold uppercase tracking-[.12em] transition hover:bg-white hover:text-[#272421]">Join the list</button></form></section></div><div className="border-t border-stone-700"><div className="mx-auto flex max-w-[1440px] flex-col gap-3 px-4 py-5 text-[11px] text-stone-400 sm:flex-row sm:items-center sm:justify-between sm:px-7"><span>© {new Date().getFullYear()} AB Collection. All rights reserved.</span><div className="flex flex-wrap gap-x-4 gap-y-2"><Link view="privacy-policy">Privacy</Link><Link view="terms-policy">Terms</Link><Link view="cancellation-policy">Cancellation</Link><Link view="returns-policy">Returns</Link><button onClick={() => navigate('admin')} className="text-left text-xs text-stone-300 transition hover:text-white">Admin</button></div></div></div></footer>;
};
